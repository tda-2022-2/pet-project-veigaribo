import { StyleSheet, View } from 'react-native'

export default function MainMenuSpacing() {
  return <View style={styles.spacing}></View>
}

const styles = StyleSheet.create({
  spacing: {
    height: 10,
  },
})
